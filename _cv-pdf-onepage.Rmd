---
# See _cv-pdf.Rmd for notes on why contact fields are duplicated here.
# This is a condensed, one-page variant: trimmed distinctions list and
# only the most-cited publications (ranked using content/scholar_pubs.csv),
# no Teaching/Presentations sections. `params.n_distinctions`/`n_pubs`
# below are the defaults when rendered directly; render_cv_pdf_onepage.R
# overrides them and searches for values that fill exactly one page.
name: "William"
surname: "Pearman"
position: "Research Fellow"
address: "Auckland, New Zealand"
email: "william.pearman@auckland.ac.nz"
github: "wpearman1996"
linkedin: "william-pearman-6b3188392"
scholar: "Gn6BJkoAAAAJ"
profilepic: "profile.jpg"
aboutme: "Molecular and microbial ecologist working on population genomics and the evolution of host-microbe (holobiont) relationships."
scholarstats: "`r scholar_stats_line()`"
date: "`r format(Sys.Date(), '%B %Y')`"
output: vitae::twentyseconds
header-includes:
  - \hypersetup{colorlinks=true, linkcolor=mainblue, urlcolor=mainblue}
params:
  n_distinctions: 6
  n_pubs: 6
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
library(vitae)
source("_common.R")
meta <- load_meta()
```

I am a broadly trained molecular and microbial ecologist specialising in population genomics and microbial ecology, developing eco-evolutionary simulations of host-microbe (holobiont) relationships tested against observational and experimental data.

# Education

```{r}
education_tibble() |>
  detailed_entries(what = degree, when = year, with = institution, where = location)
```

# Positions

```{r}
positions_tibble() |>
  detailed_entries(what = title, when = dates, with = institution, where = location)
```

# Distinctions, Grants & Memberships

```{r}
#| results: asis
cat(render_cv_distinctions(top_n = params$n_distinctions))
```

# Selected Publications

```{r}
#| results: asis
cat(render_cv_publications(top_n = params$n_pubs, rank_by = "cites"))
```
